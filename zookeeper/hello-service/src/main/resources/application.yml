spring:
  application:
    name: hello-service
server:
  port: 18000

endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true
  health:
    sensitive: false

logging.level:
  org.apache.zookeeper.ClientCnxn: ERROR

management:
  security:
    enabled: false